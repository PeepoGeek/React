import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'appPractice1',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
